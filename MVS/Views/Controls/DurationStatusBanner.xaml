<UserControl x:Class="MVS.Views.Controls.DurationStatusBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignWidth="600" d:DesignHeight="70">
    <Border BorderBrush="#D0D7DE" BorderThickness="1" CornerRadius="4" Background="#F6F8FA" Padding="12,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="Recording Duration"
                       FontWeight="SemiBold"/>

            <TextBlock Grid.Row="0" Grid.Column="1"
                       x:Name="tbDurationValue"
                       Text="--:--:--"
                       FontWeight="SemiBold"
                       Margin="8,0,0,0"/>

            <telerik:RadProgressBar Grid.Row="1" Grid.ColumnSpan="2"
                                    x:Name="pbDuration"
                                    Height="6"
                                    Minimum="0"
                                    Maximum="100"
                                    Value="0"
                                    Margin="0,6,0,4"/>

            <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                       x:Name="tbStatusMessage"
                       Text="No data recorded yet."
                       Foreground="#57606A"
                       FontSize="11"/>
        </Grid>
    </Border>
</UserControl>
