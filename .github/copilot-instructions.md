# Copilot Instructions

## Project Guidelines
- Always use Telerik-styled controls (e.g., RadComboBox, RadToggleSwitchButton, RadButton) instead of standard WPF controls in the MVS project.
- Never include project version updates when generating text for git changes commits.